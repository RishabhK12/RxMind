import 'package:flutter_test/flutter_test.dart';

void main() {
  group('ChatManager', () {
    test('insertMessage', () {}, skip: true);
    test('loadSession', () {}, skip: true);
    test('unreadable when locked', () {}, skip: true);
  });
}
